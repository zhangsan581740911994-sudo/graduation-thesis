# 毕设 / ETF（--dataset=etf），无 MuJoCo / D4RL 机器人基准。
# Windows：orbax-checkpoint 须先于 flax（避免拉取依赖 uvloop 的 0.11+）。
# 安装：pip install -U pip && pip install -r requirements-thesis.txt

numpy>=1.22,<3
pandas>=1.3
gym>=0.21,<0.26
# 训练默认可用 CPU jaxlib（PyPI）。AutoDL + Python3.12 + GPU 请用 scripts/install_jax_gpu_autodl_cp312.sh
# （官方 jax_cuda_releases 对 cp312 的 GPU jaxlib 为 0.4.29+cuda12，无 0.4.35+cuda 一体式轮子）
jax==0.4.35
jaxlib==0.4.35
orbax-checkpoint==0.5.16
flax==0.8.4
optax==0.1.9
chex==0.1.86
distrax>=0.1.3
ml_collections>=0.1.0
absl-py
tqdm
cloudpickle
wandb
python-dateutil
# CSI1000 / akshare 数据脚本：云主机上新浪 HTTP 易断，BaoStock 更稳
baostock
